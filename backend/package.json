{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:image": "docker build -t budgetboozeimage .",
    "save:image": "docker save -o budgetboozeimage.tar budgetboozeimage",
    "deploy": "scp ./budgetboozeimage.tar miller@10.0.0.212:/home/miller",
    "deploy:full": "npm run build:image && npm run save:image && npm run deploy"
  },
  "author": "Clark Miller",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "sqlite3": "^5.1.7"
  }
}
